<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Core Focus</title>
</head>
<body>
    <%- include ('partials/navbar.ejs') %>
    <div class="container mt-3">
        <h2>View Opportunities</h2>          
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Opportunity ID</th>
                    <th>Organization ID</th>
                    <th>Title</th>
                    <th>Description</th>
                    <th>Date</th>
                    <th>Time</th>
                    <th>Location</th>
                    <th>Edit</th>
                    <th>Delete</th>
                </tr>
            </thead>
            <tbody>
                
                <% for (let i =0; i < opportunities.length; i++) { %>
                    <tr>
                        <td><%= opportunities[i].opportunityId %></td>
                        <td><%= opportunities[i].organizationId %></td>
                        <td><a href="/opportunity/<%= opportunities[i].opportunityId %>" ><%= opportunities[i].title %></a></td>
                        <td><%= opportunities[i].description %></td>
                        <td><%= new Date(opportunities[i].date).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %></td>
                        <td><%= opportunities[i].time %></td>
                        <td><%= opportunities[i].location %></td>
                        <td><a href="/updateOpportunity/<%= opportunities[i].opportunityId %>">Edit</a></td>   
                        <td><a href="/deleteOpportunity/<%= opportunities[i].opportunityId %>" onclick="return confirm('Are you sure you want to delete this opportunity')">Delete</a></td>
                <% } %>

            </tbody>
        </table>
    </div>
</body>
</html>