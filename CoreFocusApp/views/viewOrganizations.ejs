<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Core Focus</title>
</head>
<body>
    <%- include ('partials/navbar.ejs') %>
    <div class="container mt-3">
        <h2>View Organizations</h2>          
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Organization ID</th>
                    <th>Organization Name</th>
                    <th>Description</th>
                    <th>Contact Email</th>
                    <th>Edit</th>
                    <th>Delete</th>
                </tr>
            </thead>
            <tbody>
                
                <% for (let i =0; i < organizations.length; i++) { %>
                    <tr>
                        <td><%= organizations[i].organizationId %></td>
                        <td><a href="/organization/<%= organizations[i].organizationId %>" ><%= organizations[i].organizationName %></a></td>
                        <td><%= organizations[i].description %></td>
                        <td><%= organizations[i].contactEmail %></td>
                        <td><a href="/updateOrganization/<%= organizations[i].organizationId %>">Edit</a></td>   
                        <td><a href="/deleteOrganization/<%= organizations[i].organizationId %>" onclick="return confirm('Are you sure you want to delete this organization')">Delete</a></td>
                    </tr>
                <% } %>

            </tbody>
        </table>
    </div>
</body>
</html>